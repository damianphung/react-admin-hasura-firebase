setup:
  addons:
  - plan: heroku-postgresql
    as: DATABASE
build: 
  docker:
    web: Dockerfile.hasura
    flyway: Dockerfile.flyway
release:
  image: flyway
  command:
    - "-url=jdbc:$DATABASE_URL -mixed=true -connectRetries=60 migrate"

