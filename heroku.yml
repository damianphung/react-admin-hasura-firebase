setup:
  addons:
  - plan: heroku-postgresql
    as: DATABASE
build: 
  docker:
    # web: Dockerfile.hasura
    flyway: Dockerfile.flyway
release:
  image: flyway
run:
  flyway:
    image: flyway
    command:
      - ./run.sh    
    
  
